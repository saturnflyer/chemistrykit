# Encoding: utf-8

module ChemistryKit
  module Config
    # Simple logging configuration class to encapsulate logging settings in the same pattern
    # as others.
    class Reporting

      def path
        'evidence'
      end
    end
  end
end
